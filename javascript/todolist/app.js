let user = prompt('What would you like to do?')
const todo = [];
while (user !== 'quit') {
    if (user === 'new'){
        let nueva = prompt('Enter new todo');
    }else if(user === list){
        console.log(todo);
    }
}