<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= college.name %> </title>
</head>
<body>
    <h1><%= college.name %></h1>
    <h3>Ubicacion: <%= college.location %></h3>
    <h3>Modalidad: <%= college.modality %></h3>

    <form action="/colleges/<%= college._id %>?_method=DELETE" method="POST">
        <button>Delete</button>
    </form>

    <ul>
        <% for(let course of college.courses) {%>
            <li><a href=""><%= course.name %></a></li>
        <% } %>  
    </ul>

    <a href="/colleges">Back to Index</a>
    <a href="/colleges/<%= college._id %>/courses/new">Add Course</a>
</body>
</html>